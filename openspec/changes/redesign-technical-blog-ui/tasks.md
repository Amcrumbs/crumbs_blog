## 1. 全局视觉系统

- [x] 1.1 在 `src/app/globals.css` 中重建语义化设计 token，覆盖浅色和暗色主题。
- [x] 1.2 更新 `surface`、`surface-strong`、`text-muted`、`text-faint`、`editorial-title` 和基础链接/选择/焦点样式。
- [x] 1.3 优化 `prose-workspace` 的标题、段落、列表、引用、链接、行内代码和代码块样式。
- [x] 1.4 扫描并替换关键组件中的高风险硬编码颜色，使 light/dark mode 下保持可读。

## 2. 全站外壳与通用组件

- [x] 2.1 调整 `AppShell` 的侧边栏、品牌区、导航项、状态区和语言切换区域，使其符合新 token 和克制交互规则。
- [x] 2.2 调整 `PageHeading`，让页面标题区更轻、更 editorial，并保留现有 props 接口。
- [x] 2.3 调整表单、按钮、输入框和标签的通用视觉，使留言、私密解锁和工具占位界面在新主题下统一。

## 3. 首页改造

- [x] 3.1 将首页主视觉改为 Editorial Hero，展示站点标题、说明和主要入口。
- [x] 3.2 将首页内容入口、近期内容、工具快捷入口和公开链接摘要组织为响应式 bento grid。
- [x] 3.3 移除或降级依赖绝对定位的知识花园节点图，确保移动端和桌面端都不重叠、不溢出。

## 4. 文章列表与卡片

- [x] 4.1 重构 `ContentCard`，统一日期、标题、摘要、标签和可点击反馈的现代卡片样式。
- [x] 4.2 调整 notes 列表页，使公开笔记以适合扫描的响应式卡片网格展示。
- [x] 4.3 调整 logs 和私密日志列表，使连续记录保持清晰阅读节奏并复用同一卡片语言。

## 5. 文章详情阅读体验

- [x] 5.1 重构 `ContentArticle` 的 header 和正文容器，弱化厚重面板感。
- [x] 5.2 优化正文最大宽度、字号、行高、段落间距、标题间距和标签展示。
- [x] 5.3 确认公开笔记、公开日志和已解锁私密日志详情页均复用新阅读体验。

## 6. 兼容页面收口

- [x] 6.1 检查 tools、links、guestbook、private 页面中复用卡片和表单的区域，做必要的视觉一致性调整。
- [x] 6.2 确认现有路由、内容读取、slug、语言切换、公开/私密访问规则不变。
- [x] 6.3 确认浅色和暗色系统主题下主要页面文本可读、边框柔和、hover/focus 清晰。

## 7. 验证

- [x] 7.1 运行 `npm run typecheck` 并修复发现的问题。
- [x] 7.2 运行 `npm run lint` 并修复发现的问题。
- [x] 7.3 运行 `npm run build` 并修复发现的问题。
